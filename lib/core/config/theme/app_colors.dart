import 'package:flutter/material.dart';

abstract class AppColors {
  // Couleurs principales
  static const Color primary = Color(0xFF373a98);
  static const Color secondary = Color(0xFFf89320);

  // Arrière-plans
  static const Color background = Color(0xFFECECF2);
  static const Color surface = Color(0xFFF8F8FF);
  static const Color drawerBackground = Color(0xFF0B0C0D);

  // Textes
  static const Color textPrimary = Color(0xFF0B0C0D);
  static const Color textSecondary = Color(0xFF212627);
  static const Color textLight = Color(0xFF7A7F85);

  // Réseaux sociaux
  static const Color googleButton = Color(0xFFFF5252);
  static const Color facebookButton = Color(0xFF4267B2);

  // États
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFFF5252);
  static const Color warning = Color(0xFFFFC107);

  // Utilitaires
  static const Color divider = Color(0xFFC6CBD2);
  static const Color inputBackground = Color(0xFFF0F2F5);
  static const Color shadow = Color(0x1A000000);
}
